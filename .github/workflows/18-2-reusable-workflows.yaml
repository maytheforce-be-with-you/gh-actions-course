name: 18 2 reuseable workflows
on: 
  workflow_dispatch: 


jobs:
  deploy: 
    uses: ./.github/workflows/18-1-reuseable-workflows.yaml
    with:
     target_dir: dummy_dir

  print-output:
    runs-on: ubuntu-latest
    needs: deploy
    steps: 
      - name: print outputs
        run: | 
          echo "build status: ${{needs.deploy.outputs.build-status}}"
          echo "url: ${{needs.deploy.outputs.url}}"


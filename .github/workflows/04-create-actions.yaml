name: 04 Create Actions

on: push

jobs:
  build:
    runs-on: ubuntu-latest
    steps: 
      - name: Check-out code
        uses: actions/checkout@v4
      - name: print the folders
        run: ls
        #dep
        #check out 
        